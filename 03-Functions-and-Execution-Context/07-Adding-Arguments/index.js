const x = 23

const isX = (num) => {
    const x = 44;
    return num === x;
}

const isY = (num) => num ===x;

const one = isX(44);
const two = isY(23);

// Uncomment these to check your work!
// console.log(one); // should log: true
// console.log(two); // should log: true